# GitHub Pages 网站部署指南

## 📖 简介

本指南将帮助你将 Awesome LLM Learning 仓库部署到 GitHub Pages，创建一个简约清晰的苹果风格网站。

**重要特性**：网站会**直接读取仓库中的原始 Markdown 文件**，无需手动复制或同步。当你更新 Markdown 文件后，推送到 GitHub 即可自动更新网站。

## 🎨 设计特点

网站采用 **VitePress** 构建，具有以下特点：

- ✨ **简约清晰**：仿照苹果设计风格，界面简洁优雅
- 🎯 **响应式设计**：完美适配桌面、平板和手机
- 🚀 **快速加载**：基于 Vite 构建，加载速度极快
- 🔍 **全文搜索**：内置搜索功能，快速查找内容
- 🌗 **深色模式**：支持亮色/暗色主题切换
- 📱 **移动优化**：移动端体验优秀
- 🔄 **实时同步**：直接读取原始 Markdown，无需手动同步

## 🚀 快速开始

### 1. 准备工作

确保已安装：
- Node.js 18 或更高版本
- Git

### 2. 本地预览

在仓库根目录执行以下命令：

```bash
# 1. 安装依赖（首次需要）
npm install

# 2. 启动开发服务器
npm run docs:dev
```

现在打开浏览器访问 `http://localhost:5173` 即可预览网站。

VitePress 会自动读取项目根目录下的所有 Markdown 文件并生成网站。

### 3. 部署到 GitHub Pages

#### 自动部署（推荐）

1. **启用 GitHub Pages**
   - 进入仓库的 `Settings` > `Pages`
   - 在 `Source` 下选择 `GitHub Actions`

2. **推送代码**
   ```bash
   git add .
   git commit -m "Add VitePress documentation site"
   git push origin main
   ```

3. **等待部署完成**
   - 前往 `Actions` 标签页查看部署进度
   - 部署完成后，网站将自动发布到：
     `https://kebijuelun.github.io/Awesome-LLM-Learning/`

## 📁 项目结构

```
Awesome-LLM-Learning/
├── .vitepress/                     # VitePress 配置
│   ├── config.mts                  # 网站配置（导航、侧边栏等）
│   └── theme/                      # 自定义主题
│       ├── index.ts                # 主题入口
│       ├── style.css               # 苹果风格样式
│       └── logo.svg                # Logo 图标
├── .github/workflows/              # GitHub Actions 配置
│   └── deploy.yml                  # 自动部署配置
├── public/                         # 静态资源
│   └── favicon.ico                 # 网站图标
├── index.md                        # 首页（直接在根目录）
├── 1.深度学习基础知识/             # 原始内容目录（直接使用）
│   ├── 1.Transformer基础.md
│   ├── 2.深度神经网络基础.md
│   └── images/                     # 图片资源
├── 2.自然语言处理基础知识/
├── 3.大语言模型基础知识/
├── 4.大语言模型推理/
├── 5.大语言模型应用/
├── 6.大语言模型前沿分享/
├── package.json                    # 依赖配置
└── README.md                       # 原始 README
```

**架构说明**：
- VitePress 直接读取根目录的 Markdown 文件
- 无需手动复制或同步文件
- 更新 Markdown 后直接推送，网站自动更新

## 📝 内容管理

### 添加新内容

1. **在对应目录添加/修改 Markdown 文件**
   ```bash
   # 例如：在深度学习基础知识目录添加新文章
   vim 1.深度学习基础知识/3.新章节.md
   ```

2. **更新导航配置**（如果需要）
   编辑 `.vitepress/config.mts`，在 `sidebar` 部分添加新条目：
   ```typescript
   {
     text: '新章节',
     link: '/1.深度学习基础知识/3.新章节'
   }
   ```

3. **提交并推送**
   ```bash
   git add .
   git commit -m "Add new content"
   git push
   ```

就这么简单！GitHub Actions 会自动构建并部署更新后的网站。

### 图片管理

- 图片应放在各章节的 `images` 目录下
- 在 Markdown 中使用相对路径引用：
  ```markdown
  ![描述](./images/图片名.png)
  ```
- VitePress 会自动处理图片路径

### 实时预览

在本地开发时，VitePress 支持热重载：

```bash
npm run docs:dev
```

修改任何 Markdown 文件后，浏览器会自动刷新显示最新内容。

## 🔧 自定义配置

### 修改网站标题和描述

编辑 `.vitepress/config.mts`：

```typescript
export default defineConfig({
  title: "你的网站标题",
  description: "你的网站描述",
  // ...
})
```

### 修改导航栏

编辑 `.vitepress/config.mts` 中的 `nav` 部分：

```typescript
nav: [
  { text: '首页', link: '/' },
  { text: '新增导航', link: '/path/to/page' },
  // ...
]
```

### 修改侧边栏

编辑 `.vitepress/config.mts` 中的 `sidebar` 部分。侧边栏会根据路径自动匹配：

```typescript
sidebar: {
  '/1.深度学习基础知识/': [
    {
      text: '深度学习基础知识',
      items: [
        { text: 'Transformer基础', link: '/1.深度学习基础知识/1.Transformer基础' },
        // 添加新条目
      ]
    }
  ]
}
```

### 修改主题颜色

编辑 `.vitepress/theme/style.css`：

```css
:root {
  --vp-c-brand-1: #007aff;  /* 主题色 */
  --vp-c-brand-2: #0070e0;  /* 主题色变体 */
  /* ... */
}
```

### 修改首页

编辑根目录的 `index.md`：

```markdown
---
layout: home

hero:
  name: "你的网站名称"
  text: "你的标语"
  tagline: "详细描述"
  # ...
---
```

## 🐛 常见问题

### 1. 网站显示 404

**问题**：访问 GitHub Pages 地址显示 404

**解决方案**：
- 确认 GitHub Pages 已启用，Source 设置为 `GitHub Actions`
- 确认 `.vitepress/config.mts` 中的 `base` 配置正确（应为 `/Awesome-LLM-Learning/`）
- 等待几分钟，GitHub Pages 部署需要时间
- 检查 Actions 标签页是否有部署错误

### 2. 图片无法显示

**问题**：网站上的图片显示不出来

**解决方案**：
- 检查图片路径是否正确（使用相对路径 `./images/xxx.png`）
- 确认图片文件存在于对应的 `images/` 目录
- 检查图片文件扩展名大小写是否一致

### 3. 样式不正确

**问题**：网站样式显示异常

**解决方案**：
- 清除浏览器缓存（Ctrl+Shift+R 或 Cmd+Shift+R）
- 重新构建：`npm run docs:build`
- 检查 `.vitepress/theme/style.css` 是否有语法错误

### 4. 构建失败

**问题**：GitHub Actions 构建失败

**解决方案**：
- 查看 Actions 标签页的错误日志
- 本地测试构建：`npm run docs:build`
- 确认 Node.js 版本兼容（需要 18+）
- 检查是否有语法错误的 Markdown 文件

### 5. 本地开发端口被占用

**问题**：运行 `npm run docs:dev` 提示端口被占用

**解决方案**：
```bash
# 指定其他端口
vitepress dev --port 5174
```

## 🔄 工作流程

### 日常更新流程

1. **编辑内容**：直接编辑根目录下的 Markdown 文件
2. **本地预览**：`npm run docs:dev` 查看效果
3. **提交推送**：
   ```bash
   git add .
   git commit -m "Update content"
   git push
   ```
4. **自动部署**：GitHub Actions 自动构建并部署
5. **查看效果**：访问 GitHub Pages 地址查看更新

### 大型更新流程

1. **创建分支**：
   ```bash
   git checkout -b feature/new-content
   ```

2. **批量修改**：添加或修改多个文件

3. **本地测试**：
   ```bash
   npm run docs:build
   npm run docs:preview
   ```

4. **提交合并**：
   ```bash
   git add .
   git commit -m "Add new features"
   git push origin feature/new-content
   # 在 GitHub 创建 Pull Request 并合并
   ```

## 📊 性能优化建议

1. **图片优化**
   - 压缩图片文件大小（推荐使用 TinyPNG）
   - 使用 WebP 格式（现代浏览器支持）
   - 控制图片尺寸（不要超过实际显示尺寸）

2. **Markdown 优化**
   - 避免单个文件过大（建议不超过 500KB）
   - 合理拆分长文章
   - 使用合适的标题层级

3. **构建优化**
   - VitePress 已自动优化，无需额外配置
   - 生成的静态文件会自动压缩和分块

## 📚 相关资源

- [VitePress 官方文档](https://vitepress.dev/)
- [GitHub Pages 文档](https://docs.github.com/pages)
- [Markdown 语法指南](https://www.markdownguide.org/)
- [Apple Design Resources](https://developer.apple.com/design/resources/)

## 🎯 下一步

完成部署后，你可以：

1. ✅ 自定义主题颜色和样式
2. ✅ 添加更多内容和章节
3. ✅ 配置自定义域名
4. ✅ 添加 Google Analytics 等分析工具
5. ✅ 优化 SEO 和元数据

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进网站！

提交 PR 时，GitHub Actions 会自动构建预览，确保没有问题。

## 📄 许可证

与主仓库保持一致。

---

**祝你使用愉快！** 🎉

如有问题，请在 GitHub Issues 中提出。
